.idea
.hugo_build.lock
# Root only: an unanchored `data` also matches static/data, which is the
# scene data the illustrations load.
/data
public
resources
themes
# notebook render output and caches
notebooks/results/
**/.ipynb_checkpoints/
__pycache__/
site/
